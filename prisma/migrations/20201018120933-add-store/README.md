# Migration `20201018120933-add-store`

This migration has been generated by dannying at 10/18/2020, 3:09:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Post" DROP CONSTRAINT "Post_authorId_fkey"

CREATE TABLE "public"."Store" (
"id" SERIAL,
"name" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."_StoreToUser" (
"A" integer   NOT NULL ,
"B" integer   NOT NULL 
)

CREATE UNIQUE INDEX "_StoreToUser_AB_unique" ON "public"."_StoreToUser"("A", "B")

CREATE INDEX "_StoreToUser_B_index" ON "public"."_StoreToUser"("B")

ALTER TABLE "public"."_StoreToUser" ADD FOREIGN KEY ("A")REFERENCES "public"."Store"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_StoreToUser" ADD FOREIGN KEY ("B")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "public"."Post"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201014193748-start..20201018120933-add-store
--- datamodel.dml
+++ datamodel.dml
@@ -3,26 +3,23 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
-model Post {
+model Store {
   id        Int     @default(autoincrement()) @id
-  title     String
-  content   String?
-  published Boolean @default(false)
-  author    User?   @relation(fields: [authorId], references: [id])
-  authorId  Int?
+  name     String
+  users     User[]
 }
 model User {
   id       Int     @default(autoincrement()) @id
   email    String  @unique
   password String  @default("")
   name     String?
-  posts    Post[]
+  stores   Store[]
   role     Role    @default(SALLER)
 }
 enum Role {
```


